[project]
name = "busca-core"
version = "0.1.0"
description = "Motor de busca FTS multi-domínio"
requires-python = ">=3.11"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
